<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBlueDark"
    tools:context=".DetailCastActivity"
    tools:ignore="HardcodedText, SpUsage, ContentDescription">

    <RelativeLayout
        android:id="@+id/dc_layout_poster"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/dc_poster"
            android:layout_width="130dp"
            android:layout_height="195dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="8dp"
            android:background="@color/colorAccent2"
            android:padding="1dp"
            android:src="@drawable/logo_accent" />

        <ImageView
            android:id="@+id/dc_poster_black"
            android:layout_width="130dp"
            android:layout_height="195dp"
            android:layout_alignStart="@id/dc_poster"
            android:layout_alignTop="@id/dc_poster"
            android:layout_alignEnd="@id/dc_poster"
            android:layout_alignBottom="@id/dc_poster"
            android:src="@drawable/bg_black" />

        <TextView
            android:id="@+id/dc_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/dc_poster"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_toEndOf="@id/dc_poster"
            android:gravity="bottom"
            android:lineSpacingMultiplier="1.2"
            android:text="Name"
            android:textColor="@color/colorWhite"
            android:textSize="22dp"
            android:textStyle="bold" />
    </RelativeLayout>

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/dc_view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/dc_navigation"
        android:layout_below="@id/dc_layout_poster" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/dc_navigation"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        app:elevation="4dp"
        app:itemIconTint="@color/colorAccent2"
        app:itemTextColor="@color/colorAccent2"
        app:menu="@menu/navigation_for_cast" />
</RelativeLayout>